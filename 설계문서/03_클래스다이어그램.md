# 클래스 다이어그램 (도메인 모델)

**프로젝트명** 그룹 여행 협업 플랫폼 (enjoy-trip)
**기준** `backend/.../domain/*/entity` JPA 엔티티 + 공통 `BaseEntity`

> 모든 엔티티는 `created_at`/`updated_at`을 가지는 추상 `BaseEntity`를 상속한다(다이어그램에서는 생략).
> 연관은 JPA 매핑 + DB FK 기준이며, 일부는 성능상 ID 참조(FK만)로 둔 곳이 있다.

---

## 1. 핵심 도메인 — 사용자·그룹·멤버십

```mermaid
classDiagram
    class BaseEntity {
        <<abstract>>
        +Long id
        +LocalDateTime createdAt
        +LocalDateTime updatedAt
    }
    class User {
        +String email
        +String name
        +String avatarKey
        +String payoutLink  「AES-GCM」
        +String payoutAccount 「AES-GCM」
        +LocalDateTime deletedAt
    }
    class OAuthIdentity {
        +OAuthProvider provider
        +String providerUserId
    }
    class RefreshToken {
        +String email
        +String token
        +LocalDateTime expiresAt
    }
    class TravelGroup {
        +String title
        +String destination
        +LocalDate startDate
        +LocalDate endDate
        +String inviteCode
        +GroupStatus status
        +String coverImageKey
        +LocalDateTime deletedAt
    }
    class GroupMember {
        +GroupRole role
        +LocalDateTime joinedAt
        +LocalDateTime leftAt
    }

    BaseEntity <|-- User
    BaseEntity <|-- TravelGroup
    User "1" --> "*" OAuthIdentity
    User "1" --> "1" RefreshToken
    TravelGroup "1" --> "*" GroupMember
    User "1" --> "*" GroupMember
```

---

## 2. 콘텐츠 도메인 — 장소·일정·투표·숙소

```mermaid
classDiagram
    class Place {
        +String googlePlaceId
        +String name
        +Double latitude
        +Double longitude
        +Double rating
        +String priceLevel
        +String photoName
    }
    class Bookmark {
        +String categoryTag
        +String memo
    }
    class Schedule {
        +LocalDate scheduleDate
        +Integer orderIndex
        +LocalTime startTime
        +LocalTime endTime
        +String title
        +Long estimatedCost
        +TransportMode transportMode
        +ScheduleStatus status
    }
    class TransportLeg {
        +String cacheKey
        +TransportMode mode
        +Integer durationSeconds
        +Integer distanceMeters
        +Integer toll
        +Integer fuelCost
        +Integer taxiFare
    }
    class VoteSession {
        +String title
        +VoteStatus status
        +Long winnerCandidateId
    }
    class VoteCandidate {
        +String memo
    }
    class Vote {
        +Integer score 「1~5」
    }
    class Accommodation {
        +String sigungu
        +BookingStatus status
        +Long reservationPrice
        +LocalDate stayDate
        +LocalDate stayEndDate
        +String bookingPhotoKey
    }

    TravelGroup "1" --> "*" Bookmark
    Place "1" --> "*" Bookmark
    TravelGroup "1" --> "*" Schedule
    Place "0..1" --> "*" Schedule : 빈 일정 허용
    TravelGroup "1" --> "*" VoteSession
    Schedule "1" --> "*" VoteSession
    VoteSession "1" --> "*" VoteCandidate
    Place "1" --> "*" VoteCandidate
    VoteCandidate "1" --> "*" Vote
    User "1" --> "*" Vote
    TravelGroup "1" --> "*" Accommodation
    Place "1" --> "*" Accommodation
```

---

## 3. 정산·알림·기타 도메인

```mermaid
classDiagram
    class Expense {
        +Long amount
        +ExpenseCategory category
        +SplitType splitType
        +LocalDate paidAt
        +Long sourceScheduleId
        +String memo
    }
    class ExpenseSplit {
        +Long owedAmount
    }
    class Settlement {
        +Long amount
        +SettlementStatus status
        +LocalDateTime senderConfirmedAt
        +LocalDateTime receiverConfirmedAt
    }
    class Notification {
        +String type
        +String message
        +String targetPath
        +LocalDateTime readAt
    }
    class UserPreference {
        +double activity
        +double food
        +double pace
        +double urbanNature
        +double timePref
    }
    class SurveyQuestion {
        +String code
        +SurveyDimension dimension
        +String content
        +boolean isReverse
    }
    class Retrospective {
        +String content
        +Integer rating 「1~5」
    }
    class GroupPhoto {
        +String objectKey
        +String contentType
    }

    TravelGroup "1" --> "*" Expense
    User "1" --> "*" Expense : payer
    Expense "1" --> "*" ExpenseSplit
    User "1" --> "*" ExpenseSplit
    TravelGroup "1" --> "*" Settlement
    User "1" --> "*" Settlement : from/to
    User "1" --> "*" Notification
    User "1" --> "1" UserPreference
    TravelGroup "1" --> "*" Retrospective
    TravelGroup "1" --> "*" GroupPhoto
    Expense ..> Schedule : sourceScheduleId(멱등)
```

---

## 4. 주요 열거형(Enum)

| Enum | 값 |
| --- | --- |
| `OAuthProvider` | GOOGLE, KAKAO |
| `GroupStatus` | PLANNING, IN_PROGRESS, COMPLETED, DELETED |
| `GroupRole` | OWNER, MEMBER |
| `ScheduleStatus` | PLANNED, VOTING, CANCELLED |
| `TransportMode` | CAR, TRANSIT, WALK |
| `VoteStatus` | OPEN, CLOSED |
| `ExpenseCategory` | MEAL, LODGING, TRANSPORT, TICKET, OTHER |
| `SplitType` | EQUAL, RATIO, AMOUNT |
| `SettlementStatus` | PENDING, SENT, COMPLETED |
| `BookingStatus` | SELECTED, BOOKED |
| `SurveyDimension` | ACTIVITY, FOOD, PACE, URBAN_NATURE, TIME_PREF |

---

## 5. 계층 구조 (레이어드 아키텍처)

```mermaid
flowchart TB
    C[Controller<br/>REST API + @RequiredGroupMember/Owner AOP]
    S[Service<br/>트랜잭션·비즈니스 로직·도메인 이벤트 발행]
    R[Repository<br/>JPA + QueryDSL]
    E[Entity<br/>도메인 모델]
    X[External Client<br/>Google/Kakao/TourAPI/Gemini]
    C --> S --> R --> E
    S --> X
    S -. ApplicationEvent .-> SSE[SseEventBridge → SseEmitter]
```
